import { CheckoutFlow } from "@/components/checkout/CheckoutFlow";

/**
 * Checkout page — server component shell.
 * Interactive multi-step flow lives in CheckoutFlow (client).
 */
export default function CheckoutPage() {
  return <CheckoutFlow />;
}
